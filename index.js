// Ordre de Priorité en CSS : !important > ID > CLasse > BaliseHTML

const sidebar = document.getElementById("side-bar");

btn.addEventListener("click", () => {
  sidebar.classList.toggle("active");
});
